fallback: true
intents:
    fallbackIntent:
        fallback: true
        initial: false
        type: text

states:
    init: 
        action: saySorry
        transitions:
            init:
                fallback: true
        initial: true
        end: true 
      
actions:   
    saySorry:
        type: text
        options:
            text: "Maaf Nada gabisa bantu :("